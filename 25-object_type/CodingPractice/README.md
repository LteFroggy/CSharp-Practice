# 코딩 연습 - 개체 만들기

교안의 예제 코드를 직접 작성하고 실행하는 프로젝트입니다.

> **참고**: 과제 사이에 변수 이름이 겹칠 수 있습니다. 이전 과제의 코드를 지우거나 주석 처리한 후 다음 과제를 작성하세요.

---

## 과제 1: 간단한 클래스와 개체

`Dog` 클래스를 정의하고 개체를 생성하여 사용하는 코드를 작성하세요.

- `Dog` 클래스에 `Name`(string), `Age`(int) 필드와 `Bark()` 메서드를 정의
- `myDog` 개체를 생성하고 이름을 "초코", 나이를 3으로 설정
- `Bark()` 메서드를 호출하고 이름과 나이를 출력

**실행 결과**

```
초코: 멍멍!
이름: 초코, 나이: 3살
```

---

## 과제 2: var 키워드로 개체 생성

`var` 키워드를 사용하여 개체를 생성하는 코드를 작성하세요.

- `Monster` 클래스에 `Name`(string) 필드를 정의
- 명시적 타입 지정 방식과 `var` 키워드 방식 각각으로 개체를 생성
- 두 개체를 `Console.WriteLine()`으로 출력

**실행 결과**

```
Monster
Monster
```

---

## 과제 3: 인스턴스 메서드

인스턴스 메서드를 가진 `Calculator` 클래스를 작성하세요.

- `Calculator` 클래스에 `Add(int x, int y)`와 `Multiply(int x, int y)` 메서드를 정의
- 개체를 생성하고 `Add(3, 5)`와 `Multiply(4, 6)`을 호출

**실행 결과**

```
합계: 8
곱: 24
```

---

## 과제 4: 인스턴스 메서드와 필드

필드에 접근하는 인스턴스 메서드를 작성하세요.

- `Counter` 클래스에 `Count`(int) 필드와 `Increment()`, `Reset()` 메서드를 정의
- `Increment()`는 `Count`를 1 증가시키고 현재 카운트를 출력
- `Reset()`은 `Count`를 0으로 초기화하고 초기화 메시지를 출력
- `Increment()`를 3번 호출한 후 `Reset()`을 호출

**실행 결과**

```
현재 카운트: 1
현재 카운트: 2
현재 카운트: 3
카운트가 초기화되었습니다.
```

---

## 과제 5: 정적 멤버와 인스턴스 멤버

정적 멤버와 인스턴스 멤버를 함께 사용하는 `GameManager` 클래스를 작성하세요.

- 정적 멤버: `TotalPlayers`(static int), `ShowStatus()` 정적 메서드
- 인스턴스 멤버: `PlayerName`(string), `Greet()` 메서드
- 정적 멤버는 `클래스명.멤버`로, 인스턴스 멤버는 `개체명.멤버`로 접근

**실행 결과**

```
[정적] 총 플레이어 수: 10
[인스턴스] 안녕하세요, 홍길동님!
```

---

## 과제 6: 여러 클래스 함께 사용

`Greeting` 클래스와 `Farewell` 클래스를 정의하고 정적/인스턴스 멤버를 호출하세요.

- `Greeting` 클래스: `SayHello()` 정적 메서드, `SayGoodbye()` 인스턴스 메서드
- `Farewell` 클래스: `Wave()` 정적 메서드, `Bow()` 인스턴스 메서드
- 각각 정적 메서드와 인스턴스 메서드를 호출

**실행 결과**

```
안녕하세요!
(손을 흔든다)
안녕히 가세요!
(인사한다)
```

---

## 과제 7: ToString() 기본 동작

`Item` 클래스의 개체를 `Console.WriteLine()`으로 출력하여 기본 동작을 확인하세요.

- `Item` 클래스에 `Name`(string), `Price`(int) 필드를 정의
- 개체를 생성하고 이름을 "포션", 가격을 100으로 설정
- `Console.WriteLine()`으로 개체를 출력

**실행 결과**

```
Item
```

---

## 과제 8: ToString() 오버라이드

`Item` 클래스에 `ToString()` 메서드를 오버라이드하여 원하는 형식으로 출력하세요.

- `Item` 클래스에 `Name`(string), `Price`(int) 필드를 정의
- `ToString()` 메서드를 오버라이드하여 `"[아이템] 이름 - 가격골드"` 형식으로 반환
- 개체를 생성하고 `Console.WriteLine()`으로 출력

**실행 결과**

```
[아이템] 포션 - 100골드
```

---

## 과제 9: ToString() 활용

`Player` 클래스에 `ToString()`을 오버라이드하여 두 명의 플레이어 정보를 출력하세요.

- `Player` 클래스에 `Name`(string), `Level`(int), `Health`(int) 필드를 정의
- `ToString()` 메서드를 오버라이드하여 `"[레벨레벨] 이름 (HP: 체력)"` 형식으로 반환
- 두 개의 개체를 생성하고 출력

**실행 결과**

```
[10레벨] 전사 (HP: 200)
[8레벨] 마법사 (HP: 120)
```

---

## 과제 10: 클래스 배열

`Enemy` 클래스 배열을 사용하여 여러 적을 관리하는 코드를 작성하세요.

- `Enemy` 클래스에 `Name`(string), `Health`(int) 필드와 `ShowInfo()` 메서드를 정의
- 크기 3의 `Enemy` 배열을 선언하고 각 요소에 개체를 생성
- `for` 문으로 배열을 순회하며 정보를 출력

**실행 결과**

```
고블린: HP 50
오크: HP 100
드래곤: HP 500
```

---

## 과제 11: foreach로 클래스 배열 순회

`Item` 클래스 배열을 `foreach`로 순회하는 코드를 작성하세요.

- `Item` 클래스에 `Name`(string), `Price`(int) 필드와 `ToString()` 오버라이드를 정의
- 개체 이니셜라이저를 사용하여 3개의 아이템으로 배열을 초기화
- `foreach`로 순회하며 출력

**실행 결과**

```
=== 인벤토리 ===
검: 500골드
방패: 300골드
포션: 50골드
```

---

## 과제 12: 개체 이니셜라이저

개체 이니셜라이저를 사용하여 `Character` 클래스의 개체를 생성하세요.

- `Character` 클래스에 `Name`(string), `Level`(int), `Job`(string) 필드를 정의
- 기존 방식과 개체 이니셜라이저 방식 각각으로 개체를 생성
- 두 개체의 정보를 출력

**실행 결과**

```
홍길동 - 전사 Lv.1
이순신 - 궁수 Lv.5
```

---

## 과제 13: 배열에서 개체 이니셜라이저 활용

`Student` 클래스 배열을 개체 이니셜라이저로 초기화하세요.

- `Student` 클래스에 `Name`(string), `Score`(int) 필드와 `ToString()` 오버라이드를 정의
- `ToString()`은 `"이름: 점수점"` 형식으로 반환
- 3명의 학생을 개체 이니셜라이저를 사용한 배열로 초기화하고 출력

**실행 결과**

```
김철수: 85점
이영희: 92점
박민수: 78점
```

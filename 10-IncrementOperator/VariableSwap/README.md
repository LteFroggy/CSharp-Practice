# 변수 값 교환

세 개의 정수 변수 `a`, `b`, `c`가 있습니다. 이 변수들의 값을 다음과 같이 순환 교환하는 프로그램을 작성하세요.

- `a`의 값은 `b`로 이동
- `b`의 값은 `c`로 이동
- `c`의 값은 `a`로 이동

초기값:
- `a = 10`
- `b = 20`
- `c = 30`

임시 변수를 사용하여 값 손실 없이 교환해야 합니다.

## 예상 실행 결과

```
교환 전: a = 10, b = 20, c = 30
교환 후: a = 30, b = 10, c = 20
```

# 코딩 연습 - 관계형 연산자와 논리 연산자

`Program.cs`에 아래 과제들의 코드를 순서대로 작성하세요.
모든 과제를 작성한 후 실행하면 각 과제의 실행 결과가 순서대로 출력됩니다.

> 💡 변수 이름이 겹치는 경우 중괄호 `{ }`로 코드를 감싸면 각 블록에서 같은 이름을 사용할 수 있습니다.

---

## 1. 관계형 연산자

### 1-1. 관계형 연산자 기본 사용법

정수형 변수 `x`에 3, `y`에 5를 저장한 후, 6가지 관계형 연산자(`==`, `!=`, `>`, `>=`, `<`, `<=`)로 비교한 결과를 각각 출력하세요.

**실행 결과**

```
False
True
False
False
True
True
```

### 1-2. 같음 연산자(==) 주의사항

정수형 변수 `score`에 100을 저장하고, `score == 100`의 결과를 `bool` 변수 `isMax`에 저장한 후 출력하세요.

**실행 결과**

```
True
```

### 1-3. 문자열 비교

`"Hello" == "Hello"`, `"Hello" == "hello"`, `"AAA" == "aaa"`의 비교 결과를 각각 출력하세요. 문자열 비교는 대소문자를 구분합니다.

**실행 결과**

```
True
False
False
```

---

## 2. 논리 연산자

### 2-1. 논리 AND 연산자 (&&)

`&&` 연산자의 진리표를 출력하세요. `true && true`, `true && false`, `false && true`, `false && false`의 결과를 문자열 보간을 사용하여 출력합니다.

**실행 결과**

```
true  && true  -> True
true  && false -> False
false && true  -> False
false && false -> False
```

### 2-2. 논리 OR 연산자 (||)

`||` 연산자의 진리표를 출력하세요. `true || true`, `true || false`, `false || true`, `false || false`의 결과를 문자열 보간을 사용하여 출력합니다.

**실행 결과**

```
true  || true  -> True
true  || false -> True
false || true  -> True
false || false -> False
```

### 2-3. 논리 NOT 연산자 (!)

`!` 연산자를 사용하여 `!true`와 `!false`의 결과를 문자열 보간을 사용하여 출력하세요.

**실행 결과**

```
!true  -> False
!false -> True
```

### 2-4. NOT 연산 예제

`bool` 변수 `isAlive`에 `false`를 저장한 후, `!isAlive`, `!!isAlive`, `!(!(!isAlive))`의 결과를 각각 출력하세요.

**실행 결과**

```
True
False
True
```

---

## 3. 연산자 조합

### 3-1. 관계형 연산자와 논리 연산자 조합

정수형 변수 `i`에 3, `j`에 5를 저장하고 `bool` 변수 `result`를 선언하세요. `(i == 3) && (j != 3)`의 결과, `(i != 3) || (j == 3)`의 결과, `(i >= 5)`의 NOT 결과를 각각 `result`에 저장하고 출력하세요.

**실행 결과**

```
True
False
True
```

### 3-2. 복합 조건 예제

`false && true`, `(1 == 1) || (1 != 1)`, `!(1 == 1)`의 결과를 각각 출력하세요.

**실행 결과**

```
False
True
False
```

### 3-3. 게임 예제: 스킬 사용 조건

정수형 변수 `level`에 10, `mp`에 50, `bool` 변수 `isStunned`에 `false`를 저장하세요. 레벨이 5 이상이고, MP가 30 이상이고, 스턴 상태가 아닐 때 스킬 사용 가능 여부를 `bool` 변수 `canUseSkill`에 저장하고 문자열 보간으로 출력하세요.

**실행 결과**

```
스킬 사용 가능: True
```

---

## 4. 단락 평가

### 4-1. 단락 평가의 활용

`string` 변수 `name`에 `null`을 저장하세요. `(name != null) && (name.Length > 0)`의 결과를 `bool` 변수 `isValidName`에 저장하고 출력하세요. 단락 평가 덕분에 `name`이 `null`이어도 오류가 발생하지 않습니다.

**실행 결과**

```
False
```

### 4-2. 비단락 연산자 (& 와 |)

`bool` 변수 `a`에 `true`, `b`에 `false`를 저장하세요. `&&`와 `&`, `||`와 `|`의 차이를 확인하기 위해 다음 8가지 결과를 출력하세요: `a && b`, `a & b`, `b && a`, `b & a`, `a || b`, `a | b`, `b || a`, `b | a`.

**실행 결과**

```
False
False
False
False
True
True
True
True
```

---

## 5. 연산자 우선순위

정수형 변수 `a`에 5, `b`에 3, `c`에 7을 저장하세요. 괄호 없이 `a > b && b < c || a == 5`의 결과를 `result1`에, 괄호를 사용하여 `((a > b) && (b < c)) || (a == 5)`의 결과를 `result2`에 저장하고 각각 출력하세요.

**실행 결과**

```
True
True
```

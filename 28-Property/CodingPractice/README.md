# 프로퍼티 사용하기 - 코딩 연습

교안의 예제 코드를 직접 작성하고 실행하는 프로젝트입니다.

> **참고**: 과제마다 동일한 이름의 변수나 클래스가 사용될 수 있습니다.
> 이전 과제의 코드를 주석 처리하거나 삭제한 후 다음 과제를 작성하세요.

---

## 과제 1: public 필드의 문제점

`Player` 클래스에 `public int Health` 필드를 선언하고, 인스턴스를 생성하여 100을 설정한 뒤 -999도 설정해 보세요. 잘못된 값이 그대로 허용되는 것을 확인합니다.

**실행 결과**

```
(문제 없이 실행되지만, 잘못된 값이 허용됨)
```

---

## 과제 2: Get/Set 메서드 방식

`Player` 클래스에 `private int _health` 필드를 선언하고, `SetHealth`와 `GetHealth` 메서드를 만들어 0~100 범위만 허용하도록 구현하세요. `SetHealth(100)`을 호출하고 결과를 출력하세요.

**실행 결과**

```
100
```

---

## 과제 3: 전체 프로퍼티

`Person` 클래스에 `private string _name` 필드와 `Name` 프로퍼티(get/set)를 만드세요. 인스턴스를 생성하여 이름을 "홍길동"으로 설정하고 출력하세요.

**실행 결과**

```
홍길동
```

---

## 과제 4: 자동 구현 프로퍼티

`Developer` 클래스에 `Name`과 `Age` 자동 프로퍼티를 선언하세요. 인스턴스를 생성하여 값을 설정하고 출력하세요.

**실행 결과**

```
김개발, 25세
```

---

## 과제 5: 자동 프로퍼티 이니셜라이저

`Player` 클래스에 기본값이 있는 자동 프로퍼티 `Name`("플레이어"), `Level`(1), `MaxHealth`(100)를 선언하세요. 인스턴스를 생성하여 기본값을 출력하세요.

**실행 결과**

```
이름: 플레이어
레벨: 1
최대 체력: 100
```

---

## 과제 6: 전체 프로퍼티 vs 자동 프로퍼티

`Car` 클래스에 전체 프로퍼티 `Name`(백킹 필드 사용)과 자동 프로퍼티 `Color`를 함께 선언하세요. 인스턴스를 생성하여 값을 설정하고 출력하세요.

**실행 결과**

```
소나타, 검정
```

---

## 과제 7: 읽기 전용 프로퍼티

`Product` 클래스에 get만 있는 읽기 전용 프로퍼티 `Manufacturer`를 만들어 "한국전자"를 반환하도록 하세요. 인스턴스를 생성하여 출력하세요.

**실행 결과**

```
한국전자
```

---

## 과제 8: private set을 사용한 읽기 전용

`Page` 클래스에 `Message` 프로퍼티(`get; private set;`)를 선언하고 기본값을 "읽기 전용 메시지"로 설정하세요. `UpdateMessage` 메서드를 만들어 내부에서만 변경할 수 있도록 하세요.

**실행 결과**

```
읽기 전용 메시지
업데이트된 메시지
```

---

## 과제 9: 쓰기 전용 프로퍼티

`SecureData` 클래스에 set만 있는 쓰기 전용 프로퍼티 `Password`와 `Validate` 메서드를 만드세요. 비밀번호를 설정하고 검증 결과를 출력하세요.

**실행 결과**

```
True
```

---

## 과제 10: 읽기 전용 식 본문 프로퍼티

`Circle` 클래스에 `Radius` 자동 프로퍼티와 식 본문(`=>`)으로 구현한 `Area`, `Circumference` 읽기 전용 프로퍼티를 만드세요. 반지름 5를 설정하고 결과를 출력하세요.

**실행 결과**

```
반지름: 5
넓이: 78.53975
둘레: 31.4159
```

---

## 과제 11: get/set 식 본문

`Counter` 클래스에 `_count` 필드와 get/set 모두 식 본문으로 작성한 `Count` 프로퍼티를 만드세요. `Increase` 메서드도 식 본문으로 작성하세요.

**실행 결과**

```
카운트: 11
```

---

## 과제 12: 프로퍼티에서 유효성 검사

`Player` 클래스에 `Health` 프로퍼티를 만들고 set에서 0~100 범위로 제한하세요. 150과 -50을 설정하여 범위 제한이 동작하는지 확인하세요.

**실행 결과**

```
체력: 100
체력: 0
```

---

## 과제 13: 계산된 프로퍼티

`Person` 클래스에 `Name` 자동 프로퍼티, `BirthYear` 쓰기 전용 프로퍼티(1900 이상만 허용), `Age` 읽기 전용 계산 프로퍼티를 만드세요. 출생년도 2000을 설정하고 나이를 출력하세요.

**실행 결과**

```
홍길동님의 나이: 25세
```

---

## 과제 14: 개체 이니셜라이저 (기존 방식)

`Course` 클래스에 `Id`와 `Title` 자동 프로퍼티를 선언하세요. 기존 방식(개체 생성 후 프로퍼티 개별 설정)으로 초기화하고 출력하세요.

**실행 결과**

```
1 - C# 기초
```

---

## 과제 15: 개체 이니셜라이저 사용

`Course` 클래스를 개체 이니셜라이저(`new Course { ... }`)로 초기화하세요. 배열에 여러 개를 생성하고 순회하며 출력하세요.

**실행 결과**

```
2 - Unity 게임 개발
1. C# 기초
2. C# 중급
3. Unity 입문
```

---

## 과제 16: 데이터 클래스

`Customer` 클래스에 `Id`, `Name`, `Email`, `City` 자동 프로퍼티를 선언하세요. 개체 이니셜라이저로 초기화하고 각 정보를 출력하세요.

**실행 결과**

```
고객 #1
이름: 김철수
이메일: kim@example.com
도시: 서울
```

---

## 과제 17: 정적 프로퍼티

`GameSettings` 클래스에 정적 자동 프로퍼티 `GameTitle`("My Game"), `MaxPlayers`(4), `IsSoundEnabled`(true)를 선언하세요. 클래스 이름으로 접근하여 값을 출력하고 변경하세요.

**실행 결과**

```
게임: My Game
최대 플레이어: 4
사운드: True
변경된 최대 플레이어: 8
```

---

## 과제 18: 프로퍼티 종합 예제

`Monster` 클래스를 만드세요. 유효성 검사가 있는 `Health` 전체 프로퍼티, `Level`/`Attack` 자동 프로퍼티, `Name` 읽기 전용 프로퍼티, `IsAlive`/`Defense` 계산된 프로퍼티, `TakeDamage` 메서드를 구현하세요. 생성자에서 이름을 받고 체력을 100으로 초기화합니다.

**실행 결과**

```
이름: 고블린
레벨: 5
체력: 100
공격력: 15
방어력: 10
생존: True
50 대미지 후 체력: 60
```
